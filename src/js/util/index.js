(function (win, l) {
  /**
   * 工具类
   * */
  class Util {
    constructor () {
      /**
       * cookie 操作对象
       * */
      this.cookie = null;
    }
  }

  Util.prototype = {
    /**
     * a 方法
     * */
    a () {

    }
  }

  l.util = new Util();
}(window, window.l || {}));